services:
  - type: web
    name: mashup-app
    runtime: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y ffmpeg
    startCommand: gunicorn webapp.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
